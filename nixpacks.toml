# Nixpacks — gunicorn only; migrate/collectstatic run via railway.toml releaseCommand
[start]
cmd = "gunicorn config.wsgi:application --bind 0.0.0.0:${PORT:-8080} --workers 1 --timeout 120 --log-file - --access-logfile -"
