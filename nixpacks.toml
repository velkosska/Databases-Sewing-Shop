# Railway / Nixpacks — Django + mysqlclient
[phases.setup]
nixPkgs = ["python311", "gcc", "pkg-config", "mysql80"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn config.wsgi:application --bind 0.0.0.0:${PORT:-8000}"
